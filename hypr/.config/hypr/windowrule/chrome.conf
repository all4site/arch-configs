# Привязка Chrome к первому рабочему столу
windowrule = match:class ^(google-chrome)$, workspace 1
windowrule =opaque on,  match:class ^(google-chrome)$


# Autostart
exec-once = google-chrome-stable

